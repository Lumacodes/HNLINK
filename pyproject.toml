[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "hn-linkedin-bot"
version = "1.0.0"
description = "HN-to-LinkedIn viral post bot with Telegram approval"
requires-python = ">=3.10"
